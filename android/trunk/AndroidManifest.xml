<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.httrack.android"
    android:versionCode="1"
    android:versionName="3.47" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="17" />

    <!--  We need to read/write copied websites to the SD card. -->
    <uses-permission 
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <!--  We need to connect to the Internet. -->
    <uses-permission 
        android:name="android.permission.INTERNET" />
    
    <!--  We need to check the Internet connection (IPv6 enabled ?). -->
    <uses-permission 
        android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" android:debuggable="false" android:description="@string/app_description">
        <activity
            android:name="com.httrack.android.HTTrackActivity"
            android:label="@string/title_activity_startup" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
