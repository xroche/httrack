# The bundled license sits beside the manual under /usr/share/doc/httrack. This
# is a pointed hint whose match context is empty, so the path lives in the
# display pointer, not the override -- match with '*'.
httrack-doc: extra-license-file *
